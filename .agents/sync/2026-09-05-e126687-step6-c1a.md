# Upstream sync report, 2026-09-05, pin `e126687a9a`, STEP 6 wave C1a

Wave `STEP6-C1A`. This wave was dispatched to re-measure the four
`vllm-online-serving` rows that [#2818](https://github.com/mudler/vllm.cpp/issues/2818)
owes at the new parity pin. **It measured one thing and it could not measure the
four rows.** The obstacle is not the fleet this time, and it is not the harness's
pin assertion, which is now satisfied. It is that three of the four rows name a
subject the committed harness cannot address, and the fourth cannot be restricted
to the two concurrencies the recipe asks for.

Issue: [#2818](https://github.com/mudler/vllm.cpp/issues/2818).
Spec: [`../specs/upstream-sync-headpin-step6.md`](../specs/upstream-sync-headpin-step6.md).
Predecessor: [`2026-09-03-e126687-step6.md`](2026-09-03-e126687-step6.md), whose
§7 C1a is the recipe this wave was to run.

| Field | Value |
|---|---|
| Active pin | `e126687a9a828d513c01a07cd69f025f27d63280` |
| Device used | `dgx:gpu0`, claimed with `rc run`, job `8c4f639c-0ebb-44de-b5e4-0e192143f7c3` |
| Rows re-measured | **none** |
| Rows newly blocked, with the reason named | 4 of 4 |
| Pin moved | NO. This wave establishes no reason to move it either way |

## 1. The two blockers the predecessor named are CLEARED

Both are confirmed by reading, not by assumption.

**The harness no longer refuses the target.** `2026-09-03-e126687-step6.md` §6
recorded that `online_gate.py:3529-3560` compares `VLLM_DISTRIBUTION_VERSION`,
`VLLM_ORACLE_VERSION`, the commit and `FLASHINFER_VERSION` against the
` ```parity-pin ` block before it reaches anything else, and that the block then
named the previous pin. The block on `origin/main` now reads:

```text
vllm_commit = e126687a9a828d513c01a07cd69f025f27d63280
vllm_runtime_version = 0.28.1rc1.dev132+ge126687a9
vllm_distribution_version = 0.28.1rc1.dev132+ge126687a9
flashinfer_version = 0.6.18
```

`e8467758e` advanced it and `5d97007c2` removed the `.precompiled` suffix from
`vllm_distribution_version`, which is the correction that lets a SOURCE build
past the equality check. The circularity §6 described is therefore resolved in
the only direction that does not weaken an assertion.

**`dgx:gpu0` is healthy.** It is heavily contended rather than down, exactly as
the predecessor's C4 predicted. This wave queued with `rc run` instead of waiting
for the box to read free.

## 2. What was measured

See §5. Nothing else in this report is a measurement, and nothing in it is quoted
as a benchmark result.

## 3. Blocker A -- three of the four rows name a subject the harness has no key for

`serve_low_common.MODEL_CHECKPOINTS` is the single record that says which weights
a model key means. (Its own comment credits #1594, which no longer resolves --
the same disappearance #2948 records for #1355, so the mechanism is cited here
from the code rather than from the tracker.) It carries five entries, and the two
committed harnesses admit these sets:

| Harness | Admitted keys |
|---|---|
| `tools/bench/online_gate.py:253` | `27`, `27n`, `35`, `q3mxfp4` |
| `tools/bench/run_serve_low.py:52` | `27`, `35`, `q38mtp` |

`Qwen/Qwen3.8-27B` bf16 @`1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0` **is in no
registry entry at all.** `q38mtp` is `r0b0tlab/Qwen3.8-27B-NVFP4-MTP-sm121`, a
different checkpoint in a different format, and the registry comment says so in
as many words.

So `docs/benchmarks/vllm-online-serving.md:36,171-179` -- the c4 row publishing
throughput **0.963x** and median ITL **1.008x**, the row the project's stated
goal turns on -- **cannot be re-measured through the committed harness.** Adding
a key is a semantic harness change and owes a spec, a red-before test and a fresh
review. This wave has no authority to make it and did not.

This is consistent with how the row was produced. `.agents/benchmark-record.md:23252`
records it under row `MODEL-Q38-27B-GATE`, and
[#2948](https://github.com/mudler/vllm.cpp/issues/2948) records that that campaign
ran through `bench.sh`, a driver on the benchmark host that this repository has
never tracked (`git rev-list --all --count -- '**/bench.sh'` is 0). The row was
never inside the committed harness, which is why the harness has no key for it.

## 4. Blocker B -- the `27n` checkpoint is not on the NAS

`docs/benchmarks/vllm-online-serving.md:74-90` is the
`nvidia/Qwen3.6-27B-NVFP4` @`0893e160` section. It carries the two throughput
rows at `:81-82` **and the cold-to-`/health` startup row at `:88`**
("33.38 s vs vLLM 182.41 s = 5.46x"). All three are the `27n` subject, so the
startup row is blocked by the same absence rather than by anything about
`nvidia-cutlass-dsl`.

The revision's three shards are 9965652512, 9985757032 and 1970287640 bytes.
A `find` over every `*.safetensors` on `/mnt/nas_share` returned 1736 files and
none of those three sizes. The search carries its own positive control: the same
listing contains the 35B shard at 10006877608 bytes once and the Qwen3.8-27B
shard at 3966730552 bytes twice, so an empty target result is an absence and not
a search that failed to run.

Staging it is a ~21.9 GB download. Every large-asset authority recorded in
`.agents/developer-preferences.md` is explicitly scoped to a named repository and
none of them covers `nvidia/Qwen3.6-27B-NVFP4`. Under AGENTS.md that gate stays
`PENDING` rather than becoming an inference.

## 5. The lease

`dgx:gpu0` was claimed with `rc run` under `setsid nohup`, `ppid == 1` verified,
never `ssh`. Two jobs were queued rather than waiting for the box to read free,
because the predecessor's C4 established that this box's obstacle is contention
and that a queued `rc` job survives its client:

| Job | Purpose |
|---|---|
| `8c4f639c-0ebb-44de-b5e4-0e192143f7c3` | the literal version read (§5.1) |
| `d7908816-96a3-40ed-8024-c3ad0cc34d77` | the 35B-A3B NVFP4 grid (§6) |

The staged wheel was verified before dispatch:
`a3cfc0e90021764f0ae671429f5c57dab47c7f10be5b96303dbc52f0a8e54688`
`vllm-0.28.1rc1.dev132+ge126687a9-cp312-cp312-linux_aarch64.whl`, 198,154,048
bytes, matching `2026-09-03-e126687-runhalf.md` §2.

### 5.1 What the version read is for, and what it must NOT be used for

`importlib.metadata.version("vllm")` is read from `cd /`, together with the
FlashInfer and runtime versions, and recorded verbatim. **If any of them differs
from the ` ```parity-pin ` block, the run stops and the difference is reported.**
The block is not edited to make a run pass. That is the #520 failure, and this
tree spent 17 days refusing the oracle its own record required rather than commit
it.

### 5.2 CUTLASS comes from the ORACLE's flashinfer, not from our own tree

`.agents/benchmark-record.md:1932-1934` pins the online-serving build to
`-DVLLM_CPP_CUTLASS_DIR=<oracle venv>/lib/python3.12/site-packages/flashinfer/data/cutlass`
-- "the oracle's own cutlass tree, not `$HOME/cutlass-4.5.0`". That is what makes
it the same input on both sides of the ratio, and it is also why FlashInfer's move
to `0.6.18` reaches our arm and not only the denominator. The dispatched job uses
that path and no other.

The job pre-builds `server` and `test_qwen36_paged_engine` at `-j 4` before
invoking the driver, so the driver's own `build_jobs=$(nproc)` finds nothing to do.
AGENTS.md records that unconstrained parallelism has OOM-rebooted this box.

## 6. Blocker C -- the one runnable subject cannot be restricted to the recipe's two cells

`nvidia/Qwen3.6-35B-A3B-NVFP4` **is** staged and its identity is confirmed by
content rather than by directory name. `/workspace/ckpt/qwen36-35b-a3b-nvfp4`
carries a `SHA256SUMS` whose three safetensors digests

```text
07141c2db92e47bc08777132cd1a0323faf300eab3a7d7c111bc2bf075fda050  model-00001-of-00003.safetensors
6dea9c759a0f941cf9e1cc1501216b0e107966a66c90425050e212efbd053f02  model-00002-of-00003.safetensors
9758875fc55e49561165f4a44342b654c123c3e25c6811b34abb83553fb1a164  model-00003-of-00003.safetensors
```

equal the LFS object ids HuggingFace reports for
`nvidia/Qwen3.6-35B-A3B-NVFP4` at `491c2f1ea524c639598bf8fa787a93fed5a6fbce`,
which is the revision key `35` pins. This matters beyond this wave: the harness
does **not** verify the snapshot. `online_gate.py:1363` sets
`model_revision = MODEL_REVISIONS[model_key]` from the registry and writes it
into the evidence, so a directory holding the wrong weights would be recorded as
the right revision and never contradicted. The snapshot's identity is the
operator's obligation, and it is discharged here by digest.

**The recipe asks for c1 and c4, and key `35` sweeps six points.**
`POINTS = ((1,6),(2,6),(4,12),(8,24),(16,96),(32,192))` and `POINTS_BY_MODEL`
carries a reduced set for `q3mxfp4` only, so `points_for("35")` returns the full
six. Running the two cells the recipe names requires either a harness change or
accepting the full six-point grid -- 336 prompts per repetition per arm, three
interleaved repetitions, two arms, after a from-scratch CUDA build. Narrowing
`POINTS_BY_MODEL` is a semantic harness change owing a spec and a fresh review, so
this wave accepted the six-point grid instead.

**How its results must be read, stated before the numbers exist rather than
after.** `c1` and `c4` are the cells `docs/benchmarks/vllm-online-serving.md:117-122`
publishes and the only ones that compare:

| | c1 | c4 |
|---|---:|---:|
| decode TPOT, published at the previous pin | 0.995x | 0.946x |
| decode throughput | 0.987x | 0.944x |
| prefill TTFT (c4 is a mean) | 0.920x | 0.849x |

**The other four rungs are ADDITIONAL, not substituted.** `:121-122` carries `-`
at c2, c8 and c32; at c16 the TPOT reads "NOT ESTABLISHED, 6.8% spread" and the
decode-throughput cell reads `0.930x` beside a TTFT that is "NOT ESTABLISHED, 15.0%
spread". So of the four extra rungs only one carries a published figure, on one axis,
and it is qualified. A ratio at those rungs therefore re-baselines rather than
re-measures, and reporting one in place of c1 or c4 would publish a
stale-denominator comparison as binding, which
`docs/benchmarks/how-we-measure.md:68-69` (M4) forbids and AGENTS.md §Gates forbids
by requiring identical concurrency on both sides.

**A stale citation, corrected rather than carried.** `2026-09-03-e126687-step6.md`
§6 and the dispatch that followed it both cite
`docs/benchmarks/how-we-measure.md:56-57` for this rule. Those lines are a note
about a sanitizer repair not applying to performance; the file is 74 lines long and
the applicable rule is M4 at `:68-69`. Cited here by content so the wrong line
number stops propagating.

**And no number from this grid is admissible until `input_lens` is checked on BOTH
arms.** #2948 records our server reporting 5,942 prompt tokens where vLLM reported
6,144 for identical prompts, which biases our own throughput UP. The committed
harness refuses that shape at `online_gate.py:771,783`, so a refusal there is the
correct outcome rather than a harness defect -- but the raw
`vllm bench serve --save-detailed` `input_lens` are to be read and reported for
each arm regardless of whether the guard fires.

## 6a. A byte discrepancy that blocked a staged-input assertion is CLOSED

Costing blocker A required identifying the bf16 checkpoint, which settled the
74,749-byte disagreement #2951 records between
`specs/qwen38-27b-bf16-gate.md:583-589` (55,586,114,863) and the campaign's
`NOTES.txt` (55,586,040,114). **Both are correct and they count different file
sets.** The staged tree is a `huggingface_hub` snapshot that omits three non-model
files and adds the downloader's cache stubs:

```
in HF, not staged:  .gitattributes 1,570 + LICENSE 11,544 + README.md 65,012 = 78,126
staged, not in HF:  .cache/huggingface/** (29 files)                          =  3,377
                                                              78,126 - 3,377 =  74,749
```

Exact, not approximate: staged-excluding-cache is 55,586,036,737, which equals the
HuggingFace all-files total minus 78,126, and adding the 3,377 gives the `NOTES.txt`
figure. Neither total is a weight total; both include the tokenizer and vocabulary,
which is why neither could serve as a staged-input assertion. The assertable
quantity is the 18 shards alone, **55,563,006,776 bytes**, whose per-shard sizes
equal HuggingFace's at `1d4bf0f2`, with
`ba0ce20a…51b1c` and `1d347950…8ea06` content-verified on shards 1 and 18 and
`77042094076611b69791a610065f28b7013b8c621795fa86ddccc8bac7d1b9df` on
`model.safetensors.index.json`. Recorded on #2951, whose subject it is.

## 7. What this wave does NOT say

It does not say the four rows are wrong, and it publishes no ratio. It reports no
red, so **nothing here is a reason to revert the pin.** #2818's rule -- that a
red re-measurement requires reverting rather than holding -- is untouched,
because no re-measurement was taken. The rows stay exactly as
`docs/benchmarks/vllm-online-serving.md:14-27` already marks them: measured at
the previous pin and owing a re-measurement.

## 8. Carry-over

**D1. The Qwen3.8-27B bf16 c4 row needs a harness key before it can be
re-measured.** §3. The decision is whether to admit `Qwen/Qwen3.8-27B`
@`1d4bf0f2` into `MODEL_CHECKPOINTS` and into `ONLINE_GATE_MODEL_KEYS`, which
would put the row inside the committed harness for the first time and, by
`online_gate.py:771,783`, inside the `input_lens` guards that
[#2948](https://github.com/mudler/vllm.cpp/issues/2948) shows the original
`bench.sh` run was never subject to. The checkpoint itself is staged and
verified: `/workspace/ckpt/qwen3.8-27b-hf`, 18 shards, 55,563,006,776 bytes,
shard 1 `ba0ce20aae489ad196733da5064bcdf159a1fe84f53336648196e1ebb7751b1c` and
shard 18 `1d3479509e21494658f9b64d317f5ea8e55c4025d28c702d6c4d0b356ce8ea06`,
both equal to HuggingFace's LFS ids at `1d4bf0f2`. Only the key is missing.

**D2. The `27n` rows and the startup row need a ~21.9 GB download authorised.**
§4. Two throughput rows and the startup row, all one subject.

**D3. The 35B grid was dispatched with the six-point sweep accepted.** §6. Its c1
and c4 are the comparable cells; c2/c8/c16/c32 are additional and re-baseline
rather than re-measure.

**D4. #2948's `input_lens` divergence is unresolved and is now directly
load-bearing for D1.** Its leading candidate is the `/v1/completions`
request-parse segment, `from_json(const nlohmann::json&, CompletionRequest&)` at
`protocol.cpp:330`, which the arm CI never reaches because it sets
`VLLM_CPP_SERVER=OFF`. If D1 is taken, the first run of the new key is also the
first time that row's prompt lengths are asserted, and the guard may refuse the
run. That refusal would be the correct outcome and should be planned for rather
than treated as a harness defect.

FOLLOWING_AGENTS_PROTOCOL
