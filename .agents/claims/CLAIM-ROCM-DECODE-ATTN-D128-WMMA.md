# CLAIM-ROCM-DECODE-ATTN-D128-WMMA

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ROCM-DECODE-ATTN-D128-WMMA` | `BACKEND-ROCM` (`ACTIVE`) | Claude Code (sonnet-5), helper role | worktree `rdna3-kernel-porting-b9ec47`, real gfx1200 hardware (AMD Radeon RX 9060 XT, RDNA4, 32 CU), `$GPU_LOCK` respected | `row/ROCM-DECODE-ATTN-D128` (same branch as `CLAIM-ROCM-DECODE-ATTN-D128`), additive not stacked; issue **TBD — owes its own, body drafted, to be filed and substituted before the spec PR merges**. Deliberately NOT [#488](https://github.com/mudler/vllm.cpp/issues/488): #488 is a measurement issue asserting no cause, this spike is one candidate answer and does not close it, and [#382](https://github.com/mudler/vllm.cpp/issues/382) already owns the scalar arm — #488 is the parent observation, referenced not claimed | Owns ONLY: `PagedAttnDecodeGqaWmmaPartial`/`PagedAttnDecodeGqaWmmaReduce`, `IsGfx1200Or1201`, `EnsureDecodeWmmaScratch`, `kDecWmmaTPar`, the `VT_ATTN_DECODE_WMMA`-gated dispatch branch, the `VT_ATTN_DECODE_WMMA` allowlist entry, and `.agents/specs/rocm-decode-attn-d128-wmma.md` + this claim. Landed **default OFF** (spike, not a production default flip — see spec §6). **NON-COLLISION:** disjoint from `CLAIM-ROCM-SKINNY-GEMM-GFX1200` (different files); additive to `CLAIM-ROCM-DECODE-ATTN-D128` on the same branch, no shared lines touched. EXCLUDED: automatic context-length dispatch, `kDecWmmaTPar` tuning, `QG=4`/`8` and Qwen3.5's `d=256` extension, fixing the pre-existing prefill-WMMA host-dispatch macro bug — since **CONFIRMED** by host-pass preprocessing (`PagedAttnPrefillSharedKWmma` has never launched at `d=256`/`d=512`; both launch sites are absent from the host translation unit while the scalar fallthrough and a runtime-gated WMMA launch are present), owed its own issue and its own red-first test, deliberately not fixed here because it is default-on merged behavior in a different code path, independently verifying the gfx1201 multi-wave `mma_sync` hang claim, and the same-tool oracle comparison (blocked this session on a Nix-glibc-vs-container-glibc ABI mismatch) | `ACTIVE` | 2026-08-12 — spike complete. Two real bugs found and fixed along the way: (1) `__gfx1200__`/`__gfx1201__` are HIP device-compilation-pass-only, never defined on the host pass, so the original `#if defined(VT_ROCWMMA_OK)` host-launch guard was silently always-false — no WMMA kernel had ever actually launched in the first two design iterations; fixed with a runtime `hipGetDeviceProperties` check. (2) The decode-only WMMA kernel was also firing on full prefill calls via the shared `flash_fallback:` fallthrough (524,288-block launches on a 2048-token prompt); fixed with a `total_q<=4` gate. Once both were fixed: `ctest` 4/4 `rocm\|cross_device` green throughout every iteration; kernel-level trace (native Nix `rocprofv3`, decode-shaped dispatches only) shows WMMA 2.36x faster than scalar at 2048-token context; wall-clock context-length sweep (Qwen3-0.6B, Qwen3-1.7B, 5 points, 2 reps each) shows a clean, mechanistically-explained crossover around 400-500 tokens — WMMA loses 3-8% below it, wins 4-27% above it, growing with context. Qwen3.5-0.8B confirmed flag-invariant (its `d=256`/`qg=4` never reaches this kernel's gate) at both ends of the sweep. |
