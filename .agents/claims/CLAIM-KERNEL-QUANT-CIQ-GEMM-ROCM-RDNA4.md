# CLAIM-KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4` | `KERNEL-QUANT-CIQ-GEMM-ROCM` (`ACTIVE`) | Claude Code (sonnet-5), single interactive session acting as implementer; fresh review by a different agent/session is still owed before this lands | local worktree `/home/justin/Projects/vllm.cpp-KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4-w1`, RX 9060 XT (gfx1200), not a fleet device — flock `$HOME/gpu.lock` for every GPU-touching command | `row/KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4-w1` (branched from `row/KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4`'s spec-only commit); issue [#2109](https://github.com/mudler/vllm.cpp/issues/2109) | Owns: `KQuantGemmKWmmaQ6K`, `KQuantGemmKWmmaQ4K`, `DequantQ6KGroup16`, `DequantQ4KTile16`, `UnpackQ4KScalesMins`, `Gfx12QuantWmmaHostOk`, `QuantWmmaEnabled`, `kQuantWmmaWarpsPerBlock`/`kGroupGemmBatch`, and the two dispatch-count witnesses, all in `src/vt/rocm/rocm_grouped_gemm.hip`; the `VT_ROCM_QUANT_WMMA` env toggle and its `docs/ENVIRONMENT.md` row; the two new `TEST_CASE`s in `tests/vt/test_backend_cross_device.cpp`; the device path added to `examples/quant_gemm_bench/main.cpp`; this row's spec and its `.agents/kernel-matrix.md` cell; and the `KERNEL` row-count bump in `scripts/check-agent-record.py`. EXCLUDES the existing scalar `KQuantGemmK`/`Dp4a`/`DotQ4K`/`DotQ6K` bodies (untouched), the Q8_K activation quantizer and its arch-selector machinery (`Q8KArchCache`, `SelectQ8KQuantArm`, untouched), the cooperative Q6_K decode arm `KQuantGemmKCoopQ6K` (untouched), and every non-gfx1200/gfx1201 ROCm target's dispatch (unaffected by construction — the host gate never selects this arm there) | `ACTIVE` | 2026-09-05 — W0-W1.4 landed as five commits on `row/KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4-w1` (not yet pushed or opened as a PR): the Q6_K WMMA tile, two performance passes on it (11.7x-16.3x over scalar), the Q4_K WMMA tile (2.9x-3.6x over scalar, both correction terms mutation-proven separately), and a run of the issue's own gate (c) recipe against the pinned `b10451` oracle (narrowly satisfied against its stated ≤20,000ms threshold; the oracle-side absolute figure does not reconcile with the issue's cited 18,812ms, recorded as open rather than elided). Hardware-verified throughout on the RX 9060 XT: both WMMA `TEST_CASE`s green, RED-first mutation-proven on every correction term, zero regression on `ctest -R 'rocm\|cross_device'` beyond one pre-existing unrelated failure (#1513/#1954). `M`/`N` tail handling and Q5_K remain `## Owed`. Still needed before this can land: the fresh-reviewer step (a different agent/session, per `AGENTS.md` "How work gets done" — this session has been sole implementer throughout) and the developer's go-ahead to push/open the PR(s) |
