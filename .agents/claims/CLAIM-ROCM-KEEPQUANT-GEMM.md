# CLAIM-ROCM-KEEPQUANT-GEMM

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ROCM-KEEPQUANT-GEMM` | `KERNEL-QUANT-CIQ-GEMM-ROCM` (`SPIKE`) | Claude Code (claude-opus-5), operator | worktree `laguna-xs-model-impl-282514`, local gfx1200 (RDNA4) + ROCm 7.2.3; no NVIDIA device and no `rc` fleet client on this host | `row/KERNEL-QUANT-CIQ-GEMM-ROCM`, base `cffe59b02` (`upstream/main`), issue [#1506](https://github.com/mudler/vllm.cpp/issues/1506), spec [rocm-keepquant-gemm.md](../specs/rocm-keepquant-gemm.md) | Owns ONLY: the new `src/vt/rocm/rocm_quant_dot.hip` and its CMake registration, the new `tests/vt/test_rocm_quant_dot.cpp` and its `tests/CMakeLists.txt` registration, the `KERNEL-QUANT-CIQ-GEMM-ROCM` kernel-matrix row + the `check-agent-record.py` `KERNEL` 56→57 bump, this claim file, the spec, and the `#1506` issue-index row. **NON-COLLISION:** the CPU keep-quant reference (this row's oracle) and `src/vt/cuda/cuda_quant_dot.cu` are UNTOUCHED — additive `kROCM` provider only; the `.hip` compiles only under `VLLM_CPP_HIP`, so the CPU and CUDA builds are byte-identical. Disjoint from [#487](https://github.com/mudler/vllm.cpp/issues/487) (M=1 decode matmuls on hipBLASLt) and [#1294](https://github.com/mudler/vllm.cpp/issues/1294) (the Q8_K activation quantizer at 35% of ROCm decode GPU time), which this row CONSUMES but does not modify. EXCLUDED: type coverage beyond Q4_K/Q6_K/Q3_K/Q5_K, wave64/CDNA, a native grouped kernel, and any speed CLAIM — the speed axis is report-only here | `SPIKE` | 2026-08-20 — spec committed before implementation, per `AGENTS.md` §"Spec before code". Nothing built. Next action is the spec's `## Tests` step 1, the red-first per-type dot over a POISONED output buffer on gfx1200 ([#1029](https://github.com/mudler/vllm.cpp/issues/1029) is why the sentinel is load-bearing rather than decorative). The registration hazard is recorded in the spec §Design: `GgufQuantComputeAvailable` probes only the DENSE op while `qwen3_5.cpp:6193` `KqGrouped` is default-ON and un-device-gated, so both ops must land together or MoE GGUFs that load today start dying at the first expert GEMM |
